






class FieldState {
   bool? hidden;
   bool? mandatory;
   String? value;
   String? error;
   String? warning;

   FieldState(
       {this.hidden, this.mandatory, this.value, this.error, this.warning});
}